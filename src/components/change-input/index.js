const createInputChange = () => {
  const inputChangeText = document.createElement('input');
  inputChangeText.classList.add('change_input');
  return inputChangeText;
};

export default createInputChange;
